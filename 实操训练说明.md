# 实操训练系统使用说明

本页面用于强化人工智能训练师三级实操题中的高频 Python API，优先覆盖 Pandas、NumPy、sklearn 数据处理与模型流程。

## 打开方式

### 推荐：双击启动

在仓库根目录双击：

`启动实操训练.bat`

浏览器将打开：

`http://127.0.0.1:8000/实操训练系统.html`

### 手动启动

在仓库根目录运行：

```bat
py -m http.server 8000
```

然后浏览器访问：

`http://127.0.0.1:8000/实操训练系统.html`

## 当前功能

- 代码填空：按中文任务填写完整 Python 表达式并自动判定。
- 函数速练：专门训练“看到中文需求，立即想到函数”。
- 模拟考试：隐藏提示，下载原始 Notebook 后在 Jupyter 中完成。
- 函数统计：记录每个函数的正确率，并自动显示薄弱函数。
- 函数速查：按中文关键词搜索常用 API。
- 原始 Notebook：保留官方/原始题目结构，方便真实运行。
- 参考答案：可直接跳转到仓库现有答案 Markdown。

## 第一版已接入题目

- 1.1.1 智能医疗系统中的业务数据处理流程设计
- 1.1.2 智能农业系统中的业务数据采集和处理流程设计
- 2.1.1 燃油效率模型的数据清洗和标注流程设计
- 2.2.1 智能信用评分 Logistic 回归模型开发与测试

这些题优先覆盖：

`pd.read_csv`、`np.where`、`value_counts`、`pd.cut`、`groupby`、`isin`、`fillna`、`drop`、`to_csv`、`isna/isnull`、`dropna`、`pd.to_numeric`、`StandardScaler`、`fit_transform`、`transform`、`train_test_split`、`LogisticRegression`、`fit`、`predict`、`accuracy_score`。

## 训练建议

建议顺序：

1. 先做“函数速练”，建立中文需求与函数名的映射。
2. 再做“代码填空”，训练完整表达式和参数写法。
3. 最后进入“模拟考试”，用原始 `.ipynb` 独立完成。

浏览器会把函数练习统计保存在 `localStorage` 中，清除浏览器数据后记录会消失。

## 后续优先扩展

下一批建议接入：

- 1.1.3 ～ 1.1.5
- 2.1.2 ～ 2.1.5
- 2.2.2 ～ 2.2.5
- 专项题：NaN / 空字符串 / `del` / `dir()` / `CountVectorizer` / 敏感词过滤
